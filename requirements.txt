# Pinned: stable, slow-moving.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1

# Pinned to the exact versions verified working end-to-end against the real
# Gemini backend (full test suite + live trips) this session. Originally
# left unpinned deliberately ("ships fast, pin later") — but that let pip's
# resolver backtrack through 60+ versions of google-genai and
# google-cloud-aiplatform one by one hunting for a combination that
# satisfies google-adk's and litellm's constraints, which turned a ~2 min
# Cloud Build into a 25-30 min one that then hit the build timeout. Pinning
# gives pip a direct install with no backtracking. Re-run
# `pip install -U` locally and re-verify before bumping these.
google-genai==2.8.0
google-adk==1.1.1
google-cloud-aiplatform==1.165.1
google-cloud-firestore==2.29.0

# Local-first dev: ADK talks to Ollama through LiteLLM. litellm needs
# Python 3.11+ (typing.NotRequired) — see .tool-versions.
litellm==1.95.1
ollama==0.6.2
# Pip's resolver sometimes settles on an adk version whose `deprecated`
# dependency doesn't get pulled in transitively when litellm's constraints
# are in the same install — pinning it directly avoids a broken import.
Deprecated==1.3.1
